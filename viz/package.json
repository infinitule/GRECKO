{
  "name": "aegisnet-c2",
  "version": "0.1.0",
  "private": true,
  "description": "AEGISNET C2 human-on-the-loop console",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.0",
    "typescript": "^5.4.5",
    "vite": "^5.2.10"
  }
}
